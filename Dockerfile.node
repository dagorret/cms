# Imagen Node ligera para compilar assets con Vite/NPM
FROM node:22-alpine

WORKDIR /var/www

# En desarrollo, mantiene a Node escuchando cambios para recarga rápida (Vite HMR)
CMD ["npm", "run", "dev", "--", "--host"]
